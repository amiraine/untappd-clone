export class Beer {
  constructor(public name: string, public brewery: string, public type: string, public abv: number, public ibu: number, public rating: number, public description: string, public notes: string) {}
}
